# Copia este archivo como .env y reemplaza los valores con tu configuración local.
# Nunca subas .env ni credenciales reales al repositorio.

NODE_ENV=development
PORT=8080
LOG_LEVEL=info

# PostgreSQL
# Ejemplo:
# DATABASE_URL=postgresql://usuario:contrasena@localhost:5432/mi_base?sslmode=disable
DATABASE_URL=